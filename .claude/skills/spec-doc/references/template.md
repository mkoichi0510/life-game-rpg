# {{Feature Name}} Specification

> 詳細仕様。実装者がコピペできるレベル。高位概要は `./overview.md` を参照。

## 1. エンティティ定義

### 主要モデル

| モデル | 主要フィールド | 説明 |
|--------|--------------|------|
| `{{ModelName}}` | {{field1, field2}} | {{役割}} |

### リレーション・制約

- {{外部キー / カスケード / 一意制約}}

詳細スキーマは [`docs/data-model.md`](../data-model.md) と `prisma/schema.prisma` を参照。

## 2. API 設計

### エンドポイント一覧

| Method | Path | 説明 | 認証 |
|--------|------|------|------|
| GET | `/api/...` | {{用途}} | 必須 / 不要 |
| POST | `/api/...` | {{用途}} | 必須 / 不要 |

### リクエスト / レスポンス例

#### `POST /api/...`

リクエスト:
```json
{
  "field": "value"
}
```

レスポンス（201）:
```json
{
  "field": "value"
}
```

### バリデーション

- スキーマ: `src/lib/validations/{{file}}.ts`
- 入力エラーは 400、認可エラーは 401/403、ドメインエラーは個別ステータス

## 3. ビジネスロジック

- {{計算式 / 条件分岐 / 副作用の順序}}
- 関連ユーティリティ:
  - `src/lib/{{path}}` — {{役割}}

## 4. 状態遷移 / ワークフロー

> 該当する場合のみ記述。状態を持たない CRUD なら本章を削除。

```
{{初期状態}} ──(操作)──> {{次状態}}
```

詳細は [`docs/state-machine.md`](../state-machine.md) を参照。

## 5. エラーハンドリング

| ドメインエラー | HTTP Status | 発生条件 |
|--------------|------------|---------|
| `{{ErrorName}}` | 4xx | {{条件}} |

エラーフォーマッタ: `src/lib/validations/helpers.ts`

## 6. 画面仕様

> 該当する場合のみ記述。API 単体機能なら本章を削除。

| 画面 | パス | 主要操作 |
|------|------|---------|
| {{画面名}} | `src/app/(main)/{{path}}/page.tsx` | {{操作}} |

UI コンポーネント・デザイン指針は [`docs/design-system.md`](../design-system.md) と [`docs/ux-spec.md`](../ux-spec.md) を参照。

## 7. テスト戦略

- ユニットテスト: `{{path}}/__tests__/*.test.ts`
- E2E テスト: `e2e/{{file}}.spec.ts`（該当する場合）
- 重点カバー範囲: {{バリデーション / 認可 / 副作用 / エッジケース}}

## 更新履歴

| 日付 | 変更内容 | 関連 PR |
|------|---------|--------|
| {{YYYY-MM-DD}} | 初版 | — |
